server:
  port: 3355

#暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: "*"  #此处有很多选项可以配置，为了省事 ,直接配置 *

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka